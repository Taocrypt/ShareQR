# ShareQR 特性介绍（中文）

ShareQR 是一个极简、即用即走的「分享→二维码」工具。它被设计为从系统分享面板直接唤起：接收文本/链接/文件等信息后，立即复制到系统剪贴板、弹出一个清爽的 MD3 风格二维码卡片，并支持保存到相册。你无需在应用内做任何额外操作，点击弹窗外即可关闭。

---

## 核心体验
- 一步直达：任意 App 内点“分享”→ 选 ShareQR → 立刻复制并生成二维码
- 无干扰的弹窗：
  - 动态取色（Android 12+），与系统主题自然融合
  - 背景高斯模糊（Android 12+），低版本用半透明遮罩降级
  - 点击弹窗外立即关闭（不打断原工作流）
- 可视化确认：底部文本框实时显示已复制内容（可选中，最多 4 行）
- 生成更快：二维码在后台线程生成，像素数组直写，渲染更高效
- 可保存传播：一键保存二维码 PNG 到系统相册，便于分享/归档

---

## 细节设计
- 视觉系统：Material Design 3（M3）
  - 使用 `textAppearanceBodyMedium` 与 `colorOnSurface`，透明度 α≈0.87，保证良好可读性
  - 自适应圆角卡片，间距精简；二维码容器白底，保证扫码对比度
- 图标显示：
  - 自适应图标（Adaptive Icon）前景放在 `drawable-nodpi`，避免密度缩放导致糊化
  - 通过 Inset（默认 12dp）控制前景留白，防止在不同启动器里“撑出”
- 兼容性：
  - Android 12+ 使用 RenderEffect 实现模糊；旧版自动降级为半透明遮罩
  - Manifest 采用 `ACTION_SEND + */*`，覆盖常见分享来源

---

## 为什么选择 ShareQR
- 即用即走：无需在应用里查找入口或配置任何东西
- 干净可靠：复制→生成→保存 都在一个轻量弹窗内完成
- 风格统一：遵循 M3 与动态取色，既现代也稳重
- 可持续迭代：
  - 可按需开启 `ACTION_SEND_MULTIPLE` 支持多条分享
  - 可替换图标前景或调整 Inset、遮罩/模糊强度、字号/行数

---

## 隐私与权限
- 仅在用户主动分享时读取传入内容
- 保存二维码时写入系统图片库
- 不采集、不上传分享内容

---

## 开源与定制
- 默认 MIT 许可
- 欢迎按你团队的品牌色与图形规范进行二次定制：图标、主题色、圆角/投影、提示样式等均可快速调整
